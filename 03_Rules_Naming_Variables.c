#include <stdio.h>

int main() {
    int num1 = 10;    // Valid variable name
    float _value = 3.14;  // Valid variable name with an underscore
    char ch = 'A';    // Valid variable name

    // Invalid variable names (uncomment to see errors)
    // int 1num = 5;      // Starts with a digit
    // float my value = 4.5; // Contains a space
    // char @ch = 'B';    // Contains a special character

    return 0;
}
